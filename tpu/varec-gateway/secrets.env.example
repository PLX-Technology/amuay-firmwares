# Copiar a /etc/varec-gateway/secrets.env  y  chmod 600
# NO se versiona: esta en .gitignore.

# Clave de la UI web (usuario: cualquiera, solo importa la clave)
VAREC_UI_PASS=cambiame

# Clave del broker MQTT, si lo pide
#VAREC_MQTT_PASS=cambiame
